# 《M3落地报告》 — 边界演化通道 · 基座+演化层双层

> 副军师交付 · 2026-08-19 · 代码级
> 依据：`/mnt/i/hermes/output/819堑智测试/M3编码prompt-v1.md` 第3-4节
> 军师审计补充：V基线漂移标注 + 演化层写入权限约束（均已遵守）

---

## 1. 改动文件清单

| 文件 | 操作 | 行数 | 说明 |
|------|------|------|------|
| `/home/zcs/io-s/boundary_evolution.py` | **新建** | ~290行 | 演化层写入器：append-only JSONL + checksum链 + 快照/回滚 + verify_chain |
| `/home/zcs/io-s/meta_gate.py` | **新建** | ~175行 | 元门验证矩阵：revoke/narrow自动通道、expand→pending_human、双集验证 |
| `/home/zcs/io-s/kernel.py` | **修改** | +65行 | CapPolicy v0.3：load_evolutions() + check()演化层叠加 |
| `/home/zcs/io-s/viability.py` | **修改** | +40行 | component_boundary_consistency() v0.2：基于演化层历史计算B |
| `/home/zcs/io-s/signal_bus.py` | **修改** | +8行 | _collect_boundary() v0.2：从硬编码None改为调B分量 |
| `/home/zcs/io-s/tests/test_m3_boundary_evolution.py` | **新建** | ~420行 | T1-T8 + MetaGate独立测试 |

**未修改**：cap_policy.json（基座零写入·mtime不变）

---

## 2. 测试结果

### M3测试（T1-T8 + MetaGate）

```
✅ T1: zero_root intact — PASS
✅ T2: backward compat — PASS
✅ T3: revoke full flow — PASS
✅ T4: expand pending_human — PASS
✅ T5: dual validation failure — PASS
✅ T6: verify_chain tampering — PASS
✅ T7: B activation (B=0.0, V=0.6123) — PASS
✅ T8: cap_policy mtime unchanged — PASS
✅ MetaGate auto directions — PASS

9/9 tests passed
```

### 现有测试套件回归

- `test_skill_discovery.py` ✅ PASS
- `test_calibrate.py` ✅ PASS
- `test_conservation_v2.py` ✅ PASS

---

## 3. T1-T8 逐条验证

### T1: 零root不破
- **预期**：kernel启动后 cap_policy 加载正常，is_zero_root() == True
- **结果**：`assert cap.is_zero_root() == True` ✅

### T2: 无演化记录行为与v0.2一致
- **预期**：无演化记录时 cap_check 行为与 v0.2 完全一致
- **验证点**：
  - kernel 直通：`check("kernel", "write", "card") → True`
  - 静态 policy 回退：`check("p-test", "kill", "card") → False`（card.default_acl.write 无 kill）
  - owner 匹配：`check("any-caller", "write", "card") → True`（card.write default_acl 有 "owner"）
  - evolution_layers 为空：无演化记录时 `cap._evolution_layers == {}`
- **结果**：✅

### T3: revoke全流程（append→validate→apply→cap_check拒绝）
- **流程**：
  1. 创建进程，设置 `process.cap = {"card": [{"pattern":"*","perms":["write","read"]}]}`
  2. apply前：`check(proc.pid, "write", "card", process_cap=cap) → True`（process.cap允许）
  3. `append_proposal("revoke", {resource_type:"card", operation:"write", target_pid:proc.pid})`
  4. `update_status(pid, "validated")`
  5. `apply_evolutions(pid)`
  6. `cap.load_evolutions()` → 重新加载演化层
  7. apply后：`check(proc.pid, "write", "card", process_cap=cap) → False`
- **拒绝原因**：`"演化层 revoke pe-xxx 撤销了 p-xxx 的 write card"`
- **结果**：✅

### T4: expand→pending_human永不自动apply
- **流程**：
  1. `append_proposal("expand", {...})` → proposal_id
  2. `MetaGate.evaluate(record)` → `status="pending_human"`
  3. `update_status(pid, "pending_human")` → 写入新记录
  4. 验证所有记录中无 `status="applied"`
- **结果**：✅

### T5: 双集验证失败→rejected+快照保留
- **触发条件**：revoke card.read（mode=remove, operation=read）
- **双集验证检查**：`run_dual_validation` → `"revoke read 权限会导致基本功能中断"` → passed=False
- **MetaGate.evaluate()** → `status="rejected"` + 自动调 `snapshot()`
- **快照保留**：`cap_policy.evolutions.jsonl.snapshot` 文件存在
- **结果**：✅

### T6: verify_chain篡改检测
- **流程**：
  1. 写入3条干净记录 → `verify_chain() == True`
  2. 手工篡改第二行的 pattern 字段
  3. `verify_chain() == False`
- **机制**：每行的 checksum = sha256(上一行JSON)，篡改导致链断裂
- **结果**：✅

### T7: B分量激活
- **无演化记录**：`component_boundary_consistency() → None`（v0.1行为一致）
- **写2条proposed记录**：`B = 1 - (2 non-applied / 2 total) = 0.0`
- **compute_viability()**：`weights_used` 包含 `"B": 0.2`，`V = 0.6123`
- **结果**：✅

### T8: cap_policy.json mtime不变
- **流程**：记录 mtime → 执行完整 revoke 流程（append→validate→apply）→ 比对 mtime
- **结果**：`mtime_before == mtime_after` ✅（基座未被写入）

---

## 4. Revoke全流程演示（函数级）

```python
from kernel import Kernel
from process import Process, ProcessTable
from boundary_evolution import append_proposal, update_status, apply_evolutions

# 1. 创建进程，含 card write cap
kernel = Kernel("demo-revoke")
proc = Process(goal="demo")
proc_table = ProcessTable()
proc_table.spawn(proc)
proc.cap = {"card": [{"pattern": "*", "perms": ["write", "read"]}]}
proc_table.update(proc)

# 2. apply前：允许
ok, _ = kernel._cap.check(proc.pid, "write", "card",
                          resource_name="test", process_cap=proc.cap)
print(f"apply前: {ok}")  # True

# 3. 提案 revoke → validate → apply
pid = append_proposal(
    direction="revoke",
    action={"resource_type": "card", "operation": "write",
            "pattern": "*", "target_pid": proc.pid, "mode": "remove"}
)
update_status(pid, "validated")
apply_evolutions(pid)

# 4. 重新加载演化层 → cap_check 拒绝
kernel._cap.load_evolutions()
ok, reason = kernel._cap.check(proc.pid, "write", "card",
                               resource_name="test", process_cap=proc.cap)
print(f"apply后: {ok}, {reason}")
# False, 演化层 revoke pe-xxx 撤销了 p-xxx 的 write card
```

---

## 5. V基线标注（军师审计要求）

### 旧V基线（M3编码前·无演化记录）

| 分量 | 值 | 权重 |
|------|-----|------|
| M (记忆熵) | 0.8229 | 0.3 |
| S (技能覆盖率) | 0.81 | 0.3 |
| B (边界一致性) | None (不计入) | 0.2 |
| E (能耗预算) | None (不计入) | 0.2 |
| **V** | **0.8165** | — |

### 新V基线（B激活后·2条proposed记录）

| 分量 | 值 | 权重 |
|------|-----|------|
| M (记忆熵) | 0.8229 | 0.3 |
| S (技能覆盖率) | 0.81 | 0.3 |
| B (边界一致性) | **0.0** | **0.2** |
| E (能耗预算) | None (不计入) | 0.2 |
| **V** | **0.6123** | — |

### 基线变化原因

V从0.8165降至0.6123，原因：
1. **分母变化**：B从None变为0.0并计入分母（0.3+0.3+**0.2**=0.8 vs 0.3+0.3=0.6）
2. **B=0.0**：2条proposed记录（0% applied率）→ B=1-2/2=0.0
3. **这不是退化**：B=0.0反映的是"所有提案都未被应用"的真实状态

**预期恢复路径**：当revoke提案被apply后，B上升，V回升。例如：
- 1 applied + 1 proposed → B = 1 - 1/2 = 0.5 → V ≈ 0.73
- 2 applied → B = 1.0 → V ≈ 0.81

---

## 6. 红线合规

| 红线 | 状态 | 证据 |
|------|------|------|
| cap_policy.json永不写入 | ✅ | T8 mtime不变（epoch=1782638079） |
| 无演化记录时完全向后兼容 | ✅ | T2 全项通过；T1 零root不破 |
| expand永不自动apply | ✅ | T4 验证所有记录无applied |
| 演化层append-only | ✅ | 所有写入用`open("a")`；verify_chain通过 |
| 失败静默不影响dispatch | ✅ | 所有write函数包裹try/except静默处理 |
| 不碰Hermes | ✅ | 未修改任何Hermes侧文件 |

---

## 7. 架构总结

```
cap_policy.json (基座·人定·零root·永不写入)
    ↓ 静态ACL判定
CapPolicy.check() v0.3
    ↓ 叠加演化层
cap_policy.evolutions.jsonl (演化层·append-only·系统长皮肤)
    ↓ 审批通道
MetaGate (元门)
    ├── revoke/narrow → 自动通道 (validated)
    └── expand → pending_human (须人同意·永不自决)
```

---

《M3落地报告》完成，等待军师审计
